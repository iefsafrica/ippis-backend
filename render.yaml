services:
  - type: web
    name: ippishr-backend
    env: node
    plan: free
    region: ohio 
    buildCommand: |
      npm ci
      npx prisma generate
      npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
